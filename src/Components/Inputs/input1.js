import React from 'react'
function input1(props) {
    const {type, value, onChange, placeholder, classes} = props
    return (
        <div>
            <input type={type} value={value} onChange={onChange} placeholder={placeholder} className={'D1eV2TUEikjfnM ' + classes}/>
        </div>
    )
}

export default input1

